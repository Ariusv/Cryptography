<Window x:Class="TabMenu.TableWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TabMenu"
        mc:Ignorable="d"
        Title="TableWindow" Foreground="White" Background="Black" Height="408.857" Width="427.143">
    <Grid>
        <DataGrid x:Name="Items" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Letter" Binding="{Binding Key}" />
                <DataGridTextColumn Header="Code" Binding="{Binding Value}" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
